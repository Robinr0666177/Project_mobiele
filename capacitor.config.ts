import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ceustermans.robin.verzamelkaarten',
  appName: 'Verzamelkaarten',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
