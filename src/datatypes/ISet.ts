export interface ISet {
  id?: number;
  title: string;
  language: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  release_year: number; //for now I use the exact same name used in supabase
}

